# File-Input-Output-Operations
Writeinga Java program that reads data from a text file and writes it to another text file, converting all characters to uppercase and implementing exception handling for file-related exceptions.
